# Ember

Wildfire home-readiness copilot — frontend shell (landing + address entry).

```bash
npm install
npm run dev
```

The map uses Leaflet + OpenStreetMap (Carto dark tiles). Address lookup uses Nominatim, with Photon as a fallback. No API key required.

Routes: `/` landing, `/app` address search, `/app/results` placeholder.
