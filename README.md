# Ember

Wildfire home-readiness copilot — frontend shell (landing + address entry).

```bash
cp .env.example .env
# Add your public Mapbox token to VITE_MAPBOX_TOKEN
npm install
npm run dev
```

Routes: `/` landing, `/app` address search, `/app/results` placeholder.
